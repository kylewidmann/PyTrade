[tool.poetry]
name = "pytrade"
version = "0.1.0"
description = "Core trading abstractions — strategies, indicators, instruments, and broker interfaces"
authors = ["Kyle Widmann <Kyle.Widmann@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^2.2.2"
pandas = "^2.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
